# Create a program that has a function called
# factorial() that receives 2 parameters:
# the 1st one that indicates the number to calculate
# and the 2nd one called "show", that will be a logic (optional) value
# indicating if will or won't be shown at the screen the process
# of the calculation of the factorial.
