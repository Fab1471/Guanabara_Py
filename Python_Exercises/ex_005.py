#Make a program which reads an int number and show at the screen it's successor and predecessor. .")
gn = int(input('type a number: '))
pd = (gn - 1)
sc = (gn + 1)
print(f"The given number is,{gn}. It's predecessor is, {pd}. It's successor is, {sc}")
#fiz certinho, tbm é possível fazer sem variáveis, aplicando somente
#gn-1 e gn+1 dentro das máscaras de pd e sc. porém com variáveis fica mais útil. .
 