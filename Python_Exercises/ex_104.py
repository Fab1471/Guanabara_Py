# Create a program that has the function readint(),
# that will word similar to the function input() from Python,
# but making the validation to accept just 1 numeric value.

# ex:
# n = readint('Type a n')
